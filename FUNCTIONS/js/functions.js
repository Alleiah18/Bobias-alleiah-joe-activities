function normal() {
    withParameter("Alleiah joe", "Bobias");
}

function withParameter(fname, lname) {
    alert("Hello " + fname + " " + lname);
}

function getSum(num1, num2) {
    const sum = num1 + num2;
    return sum;
}